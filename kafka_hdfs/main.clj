(ns kafka-hdfs.main
    (:import
        [java.net URI]
        [org.apache.hadoop.conf Configuration]
        [org.apache.hadoop.fs FileSystem Path RawLocalFileSystem]
    )
    (:gen-class)
)

(defn -main [& args]
)
